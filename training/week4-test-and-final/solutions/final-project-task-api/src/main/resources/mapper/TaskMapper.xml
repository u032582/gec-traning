<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
  tasks テーブルへのSQLを書くファイル。
  namespace は対応する Mapper インタフェースの完全名（パッケージ込み）。
  各 <select>/<insert>… の id は、インタフェースのメソッド名と一致させる。
-->
<mapper namespace="com.example.training.task.TaskMapper">

    <!--
      列名(snake_case)と Javaフィールド(camelCase)の対応。
      application.yml で map-underscore-to-camel-case を有効にしているため
      ほとんど自動だが、明示しておくと読み手に親切。
    -->
    <resultMap id="taskResultMap" type="com.example.training.task.Task">
        <id     property="id"          column="id"/>
        <result property="title"       column="title"/>
        <result property="description" column="description"/>
        <result property="status"      column="status"/>
        <result property="dueDate"     column="due_date"/>
        <result property="createdAt"   column="created_at"/>
        <result property="updatedAt"   column="updated_at"/>
    </resultMap>

    <select id="findAll" resultMap="taskResultMap">
        SELECT id, title, description, status, due_date, created_at, updated_at
        FROM tasks
        ORDER BY id
    </select>

    <select id="findById" resultMap="taskResultMap">
        SELECT id, title, description, status, due_date, created_at, updated_at
        FROM tasks
        WHERE id = #{id}
    </select>

    <!--
      useGeneratedKeys="true" と keyProperty="id" で、
      INSERT で自動採番された id を、渡した Task オブジェクトの id に書き戻す。
    -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tasks (title, description, status, due_date)
        VALUES (#{title}, #{description}, #{status}, #{dueDate})
    </insert>

    <update id="update">
        UPDATE tasks
        SET title       = #{title},
            description = #{description},
            status      = #{status},
            due_date    = #{dueDate},
            updated_at  = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM tasks
        WHERE id = #{id}
    </delete>

</mapper>
